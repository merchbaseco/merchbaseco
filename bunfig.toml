[install]
exact = true
frozenLockfile = true
auto = "disable"
ignoreScripts = true
saveTextLockfile = true
minimumReleaseAge = 259200

[install.scopes]
merchbaseco = { url = "https://npm.pkg.github.com", token = "$NPM_TOKEN" }
